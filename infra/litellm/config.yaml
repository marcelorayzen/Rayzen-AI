model_list:
  # Chat principal — Groq 70b apenas (sem fallback pago)
  - model_name: gpt-4o
    litellm_params:
      model: groq/llama-3.3-70b-versatile
      api_key: os.environ/GROQ_API_KEY

  # Chamadas leves — Groq 8b apenas (sem fallback pago)
  - model_name: gpt-4o-mini
    litellm_params:
      model: groq/llama-3.1-8b-instant
      api_key: os.environ/GROQ_API_KEY

  # Aliases diretos para Claude — use quando quiser forçar melhor qualidade
  - model_name: gpt-4o-fable
    litellm_params:
      model: anthropic/claude-fable-5-20251101
      api_key: os.environ/ANTHROPIC_API_KEY

  - model_name: gpt-4o-premium
    litellm_params:
      model: anthropic/claude-sonnet-4-6
      api_key: os.environ/ANTHROPIC_API_KEY

  - model_name: gpt-4o-mini-premium
    litellm_params:
      model: anthropic/claude-haiku-4-5-20251001
      api_key: os.environ/ANTHROPIC_API_KEY

  # Ollama local — tarefas mecânicas (classify, extract, gap analysis, proactive)
  - model_name: gpt-local
    litellm_params:
      model: ollama/llama3.2:3b
      api_base: http://ollama:11434
      api_key: "none"

  # Fallback do gpt-local: se Ollama cair, usa Groq
  - model_name: gpt-local
    litellm_params:
      model: groq/llama-3.1-8b-instant
      api_key: os.environ/GROQ_API_KEY

  # Speech to Text
  - model_name: whisper-1
    litellm_params:
      model: groq/whisper-large-v3-turbo
      api_key: os.environ/GROQ_API_KEY

  # Text to Speech
  - model_name: tts-1
    litellm_params:
      model: groq/playai-tts
      api_key: os.environ/GROQ_API_KEY

  # Embeddings (Jina via HTTP direto — não passa pelo LiteLLM)
  - model_name: embedding
    litellm_params:
      model: openai/text-embedding-3-small
      api_key: os.environ/OPENAI_API_KEY

litellm_settings:
  cache: true
  cache_params:
    type: redis
    host: redis
    port: 6379
    ttl: 300            # 5 min — respostas idênticas reutilizadas
  success_callback: ["langfuse"]
  failure_callback: ["langfuse"]

general_settings:
  master_key: os.environ/LITELLM_MASTER_KEY

router_settings:
  routing_strategy: least-busy
  allowed_fails: 2
  cooldown_time: 60
  # Fallback automático: se Groq rate-limitar ou falhar, cai no Claude
  fallbacks:
    - {"gpt-local": ["gpt-4o-mini"]}
